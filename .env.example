# --- Discord-Bot ---
DISCORD_TOKEN=あなたのBotトークン

# --- 音声認識 ---
OPENAI_API_KEY=（音声認識にOpenAI Whisper APIを使う場合のみ）

# --- 音声生成 ---
# 音声合成方式: gtts=ピュアPython音声合成ライブラリgTTSで生成 / voicevox=VOICEVOXサーバーで生成
TTS_PROVIDER=gtts
# 話者情報などを保存するディレクトリ（省略時は ./data）
BOT_STATE_DIR=data          
#  GUIで設定を有効化するための暗号
GUI_ADMIN_TOKEN=yourt-password

# --- 音声生成:ピュアPython音声合成ライブラリgTTSで生成 ---
TTS_LANG=ja
TTS_TEMPO=0.7

# --- 音声生成:VOICEVOXサーバーで生成 ---
VOICEVOX_BASE_URL=http://127.0.0.1:50021
VOICEVOX_DEFAULT_SPEAKER=2
VOICEVOX_TIMEOUT=15

# --- ログ出力ディレクトリ ---
LOG_DIR=logs

# --- CCFOLIA ブリッジ ---
# CCFOLIA_POST_SECRET は `cat /dev/urandom | base64 | head -c 64`などで生成
CCFOLIA_BRIDGE_HOST=ココフォリアからの接続用APIのIPアドレスあるいはホスト名
CCFOLIA_BRIDGE_PORT=ココフォリアからの接続用APIのポート番号
CCFOLIA_POST_SECRET=ココフォリアから取得送付する通信で使用する秘密鍵(Tampermokeyスクリプトには平文で書かれる値)
CCFOLIA_ACCEPT_FROM=ココフォリアを起動するPC

# ミラー先（テキスト）チャンネルID（※必須）
CCFOLIA_MIRROR_CHANNEL_ID=あなたがココフォリアの書き込みを送付したいDiscordボイスチャットID

# 送信方式: file=音声ファイルを添付 / voice=ボイスCHで再生 / off=テキストのみ
CCFOLIA_TTS_MODE=voice

# VOICEVOX の話者マッピング（ココフォリアの表示名 → VOICEVOX style_id）
# 例: PL1→四国めたん（ノーマル）=2, GM→ずんだもん(あまあま)=3 等はあなたの環境に合わせて
CCFOLIA_SPEAKER_MAP_JSON={"GM":109}

# VOICEVOX のデフォルト話者（マップに無かった時のフォールバック）
CCFOLIA_DEFAULT_SPEAKER=2